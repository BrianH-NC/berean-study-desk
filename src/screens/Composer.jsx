import Placeholder from '../components/Placeholder'

export default function Composer() {
  return <Placeholder title="New entry" note="The one-box composer that parses scripture references as you type — coming in a later pass, once the notebook schema is built." />
}
