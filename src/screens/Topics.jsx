import Placeholder from '../components/Placeholder'

export default function Topics() {
  return <Placeholder title="The index" note="Edwards' running index, kept automatically — every entry by topic and by scripture book — coming in a later pass." />
}
