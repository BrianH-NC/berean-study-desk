import Placeholder from '../components/Placeholder'

export default function Entry() {
  return <Placeholder title="Entry" note="A single notebook entry with its source, cross-references, and index counts — coming in a later pass." />
}
